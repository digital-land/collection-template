name: Call Collection Run
on:
  schedule:
    - cron: 0 0 * * *
  workflow_dispatch: null
jobs:
  call-workflow:
    uses: digital-land/collection-template/.github/workflows/callable_run.yml@main
    with:
      CALLER_GITHUB_REF: ${{ GITHUB_REF }}
    secrets: inherit

